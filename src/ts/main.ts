export function bootstrap(template: any) {
    console.log(template);
    // const {portfolio, profile, career, social} = template;
    // const selector = new Selector();
    // const box: any = selector.getElements('#display-box');
    // box.innerHTML = profile;
    
    // const menuItems: any = selector.getElements('.menu-item');
    // const body: any = selector.getElements('body');
    // body.addEventListener('wheel', (ele: Element) => {
    //     console.log(ele);
    // });

    // menuItems.forEach((item: Element) => {
    //     item.addEventListener('click', (ele) => {
    //         const target: any = ele.target;
    //         switch (`${target.innerHTML}`.toLocaleLowerCase()) {
    //             case 'profile': box.innerHTML = profile;break;
    //             case 'career': box.innerHTML = career;break;
    //             case 'portfolio': box.innerHTML = portfolio;break;
    //             case 'social': box.innerHTML = social;break;
    //             default: box.innerHTML = social;
    //         }
    //     });
    // });
}